'use strict';

// модуль функции обратного вызова изменения значения зависимого поля
(function () {

  /* window.synchronizeFields = function (element, color, callback) {
    element.addEventListener('click', function () {
      return callback(element, color);
    });
  };*/

})();
